[package]
name = "aether-platform"
version.workspace = true
edition.workspace = true

[features]
timer-clock = []
alloc = ["aether-core/alloc"]
std = ["alloc", "aether-core/std"]
embedded = ["dep:embedded-hal"]
ufmt = ["dep:ufmt"]

[dependencies]
aether-core = { path = "../core", default-features = false }
embedded-hal = { version = "1.0", optional = true }
ufmt = { version = "0.2", optional = true }

[target.'cfg(target_arch = "avr")'.dependencies]
avr-device = { version = "0.9", features = ["atmega328p", "rt"] }
arduino-hal = { git = "https://github.com/SebastianSpeitel/avr-hal", features = [
    "arduino-nano"
] }

[[example]]
name = "embedded_button_led"
required-features = ["embedded"]
