[package]
name = "aether-core"
version.workspace = true
edition.workspace = true

[features]
default = ["std"]
std = ["aether-time/std"]

[dependencies]
aether-time = { path = "../time", default-features = false }
