[package]
name = "aether-core"
version.workspace = true
edition.workspace = true

[features]
alloc = []
std = ["alloc", "aether-time/std"]

[dependencies]
aether-time = { path = "../time", default-features = false }
