[package]
name = "aether-compat"
version.workspace = true
edition.workspace = true

[dependencies]
aether-core = { path = "../core" }

[dev-dependencies]
aether-platform = { path = "../platform" }
aether-task = { path = "../task" }
